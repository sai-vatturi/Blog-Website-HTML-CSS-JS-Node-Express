<!-- blogPost.ejs -->
<% blogPosts.forEach(function(post) { %>
    <div class="blog-post">
        <h2><%= post.title %></h2>
        <p><%= post.blogtext %></p>
        <p>Posted by: <%= post.name %></p>
    </div>
    <hr>
<% }); %>
